class TableTag {
    constructor(
        color,
        label,
        icon
    ) {
        this.color = color || '#2ec589'
        this.label = label || ' ',
        this.icon = icon || ""
    }
}
export default TableTag